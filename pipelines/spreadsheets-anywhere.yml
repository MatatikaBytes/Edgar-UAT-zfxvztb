version: pipelines/v0.1
label: 02. Larger CSV
timeout: 0
max_retries: 0
script: meltano run --full-refresh --force --state-id-suffix "$PIPELINE_ID" tap-spreadsheets-anywhere target-motherduck
data_components:
- tap-spreadsheets-anywhere
- MotherDuck
